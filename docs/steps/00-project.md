# 00. Godot プロジェクトを作る

## 到達点

空の Godot プロジェクトを実行できる状態にします。

## 作成するもの

- `project.godot`
- `scenes/main.tscn`
- `scripts/main.gd`

最初の Scene は `Node2D` 一つで十分です。円の描画には `Node2D` が継承している `CanvasItem` の描画機能を使用します。

## 確認

プロジェクトを実行し、エラーなく空のウィンドウが表示されれば完了です。
